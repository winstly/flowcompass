{
  "name": "winstly@flowcompass",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "description": "Standard software engineering lifecycle CLI tool — 8-stage Command/Skill system",
  "main": "dist/cli/index.js",
  "bin": {
    "flowcompass": "bin/cli.js"
  },
  "files": [
    "dist",
    "bin",
    "config",
    "schemas"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.4.3",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.2",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.6"
  }
}
